#include <stdio.h>
#include <stdlib.h>
int main()
{
    float num=1.5, numScan;
    printf("digite um numero float: ");
    scanf("%f", &numScan);

    if(num isntanceof numScan)
    {
        printf("yes, it is");
    }else{
        printf("no, it isn't");
    }
    return 0;
}
